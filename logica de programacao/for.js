/* for (let i = 10; i >= 0; i--) {
  const n = i % 2 === 0 ? 'par' : 'impar';
  console.log(n);
} */

const frutas = ['pessego', 'maçã', 'banana', 'abacaxi', 'melão'];

for (i = 0; i < frutas.length; i++) {
  console.log(frutas[i]);
}